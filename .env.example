# Copy to .env and fill in your key. The dashboard works without this;
# only the AI Chat tab is affected (falls back to free rule-based engine).
# Get a free key at: https://aistudio.google.com/apikey
GOOGLE_API_KEY=
